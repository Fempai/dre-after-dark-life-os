/* Dre After Dark · Life OS — Navigation Memory */
(()=>{'use strict';const K='dreLifeOS_activeView';function activate(id){if(!id)return false;let view=document.getElementById(id),btn=document.querySelector(`nav button[data-view="${CSS.escape(id)}"]`);if(!view||!btn)return false;document.querySelectorAll('nav button,.view').forEach(x=>x.classList.remove('active'));btn.classList.add('active');view.classList.add('active');return true}function boot(){document.addEventListener('click',e=>{let b=e.target.closest('nav button[data-view]');if(b)localStorage.setItem(K,b.dataset.view)},true);activate(localStorage.getItem(K)||'today')}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();window.LifeNavigation={activate,current:()=>localStorage.getItem(K)||'today'};})();