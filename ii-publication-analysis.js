/* Dre After Dark · Life OS — I&I Publication Analysis */
(()=>{
'use strict';
const KEY='dreLifeOS_ii', $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function load(){try{let d=JSON.parse(localStorage.getItem(KEY)||'null')||{};d.content=Array.isArray(d.content)?d.content:[];d.metrics=Array.isArray(d.metrics)?d.metrics:[];return d}catch{return{content:[],metrics:[]}}}
function bucket(dt){if(!dt)return'Unknown';let h=new Date(dt).getHours();return h<6?'Overnight':h<12?'Morning':h<17?'Afternoon':h<22?'Evening':'Late night'}
function render(){let host=$('#iiPublicationAnalysis');if(!host)return;let d=load(),pub=d.content.filter(c=>c.publishedAt||c.status==='Published');let rows=pub.map(c=>{let ms=d.metrics.filter(m=>m.contentId===c.id),latest=ms.at(-1),eng=latest?(+latest.likes||0)+(+latest.comments||0)+(+latest.saves||0)+(+latest.shares||0):null;return{c,latest,eng,time:bucket(c.publishedAt)}});let groups={};for(const r of rows){let k=r.time;groups[k]??={n:0,views:0,eng:0,samples:0};groups[k].n++;if(r.latest){groups[k].views+=+r.latest.views||0;groups[k].eng+=r.eng||0;groups[k].samples++}}host.innerHTML=`<article class="card"><p class="eyebrow">PUBLICATION → RESPONSE</p><h3>Timing and audience patterns</h3><p class="quiet">Compare publication timing with observed response. These are descriptive patterns, not proof that timing caused performance.</p>${Object.keys(groups).length?Object.entries(groups).map(([k,g])=>`<div class="ii-item"><b>${esc(k)}</b><p><small>${g.n} published · ${g.samples} measured${g.samples?` · avg ${Math.round(g.views/g.samples)} views · ${(g.eng/g.samples).toFixed(1)} engagements`:''}</small></p></div>`).join(''):'<p class="quiet">Publish content to begin the timing analysis.</p>'}${rows.length?`<div class="tablewrap"><table><thead><tr><th>Artifact</th><th>Type</th><th>Timing</th><th>Views</th><th>Engagement</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r.c.title)}</td><td>${esc(r.c.type||'Content')}</td><td>${esc(r.time)}</td><td>${r.latest?.views??'—'}</td><td>${r.eng??'—'}</td></tr>`).join('')}</tbody></table></div>`:''}</article>`}
function install(){let root=$('#iiRoot');if(!root)return false;let host=$('#iiPublicationAnalysis');if(!host){host=document.createElement('div');host.id='iiPublicationAnalysis';let chain=[...root.querySelectorAll('article')].find(a=>a.textContent.includes('RESEARCH THREAD'));if(chain)chain.insertAdjacentElement('beforebegin',host);else root.appendChild(host)}render();return true}
function boot(){let n=0,t=setInterval(()=>{if(install()||++n>30)clearInterval(t)},250)}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();window.addEventListener('lifeos:ii-ready',install);window.addEventListener('lifeos:rerender',()=>setTimeout(install,0));
})();