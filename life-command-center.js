/* Dre After Dark · Life OS — Premium Command Center */
(()=>{'use strict';const K='dreLifeOS_commandCenter',$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),load=()=>{try{return JSON.parse(localStorage.getItem(K)||'{"chronicle":[],"cabinet":[],"experiments":[]}')}catch{return{chronicle:[],cabinet:[],experiments:[]}}},save=d=>localStorage.setItem(K,JSON.stringify(d));
const tools=[['Chronicle','A running record of milestones, changes and memorable events.'],['Cabinet','Save products, objects, references, links and things worth remembering.'],['Experiments','Run small life experiments with a question, change and outcome.'],['Compare Mode','Use dated records and photos to compare periods instead of relying on impressions.'],['Data Explorer','Surface the underlying logs behind dashboards, trends and insights.'],['Dashboard Builder','Choose which domains deserve attention without making every tool visible all the time.'],['Data Health','Spot missing dates, incomplete records and gaps before drawing conclusions.'],['Private Story Studio','Turn your own data into personal or shareable visual retrospectives.']];
function capture(kind){let d=load(),text=prompt(`Add to ${kind}`);if(!text)return;(d[kind.toLowerCase().replace(/\s+/g,'')]??=[]).push({at:new Date().toISOString(),text});save(d);render()}
function render(){let h=$('#commandCenter');if(!h)return,d=load();h.innerHTML=`<article class="card"><p class="eyebrow">LIFE LAB · PREMIUM TOOLKIT</p><h2>Command Center</h2><p>Useful complexity, progressively disclosed: the sophisticated tools are here when you want them without crowding Today.</p><div class="grid two">${tools.map(([n,x])=>`<div class="ii-item"><b>${esc(n)}</b><p><small>${esc(x)}</small></p>${['Chronicle','Cabinet','Experiments'].includes(n)?`<button data-capture="${n}">+ Add</button>`:''}</div>`).join('')}</div></article>`;h.querySelectorAll('[data-capture]').forEach(b=>b.onclick=()=>capture(b.dataset.capture))}
function install(){let a=$('#archive');if(!a)return false;let h=$('#commandCenter');if(!h){h=document.createElement('div');h.id='commandCenter';a.appendChild(h)}render();return true}function boot(){let n=0,t=setInterval(()=>{if(install()||++n>30)clearInterval(t)},200)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();})();