/* Dre After Dark · Life OS — Discovery / Chronicle / Cabinet / Branded Share UI */
(()=>{const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),open=h=>{let m=$('#modal'),c=$('#modalContent');if(c)c.innerHTML=h;m?.classList.remove('hidden')};function shareCard(title,body,footer='DRE AFTER DARK · LIFE OS'){let c=document.createElement('canvas');c.width=1080;c.height=1350;let x=c.getContext('2d'),g=x.createLinearGradient(0,0,1080,1350);g.addColorStop(0,'#160d14');g.addColorStop(1,'#2c1724');x.fillStyle=g;x.fillRect(0,0,1080,1350);x.strokeStyle='#b98b73';x.lineWidth=3;x.strokeRect(58,58,964,1234);x.fillStyle='#d6b59f';x.font='32px serif';x.fillText('☾  DRE AFTER DARK',95,130);x.fillStyle='#f3e8e1';x.font='bold 64px serif';wrap(x,title,95,260,890,78);x.fillStyle='#d8c9c2';x.font='36px sans-serif';wrap(x,body,95,470,890,52);x.fillStyle='#b98b73';x.font='26px sans-serif';x.fillText(footer,95,1245);x.fillStyle='#806252';x.font='22px sans-serif';x.fillText('A LIFE, DELIBERATELY ARRANGED.',95,1282);return c}function wrap(ctx,t,x,y,w,lh){let words=String(t).split(/\s+/),line='';for(let n=0;n<words.length;n++){let test=line+words[n]+' ';if(ctx.measureText(test).width>w&&n){ctx.fillText(line,x,y);line=words[n]+' ';y+=lh}else line=test}ctx.fillText(line,x,y)}async function share(title,body){let c=shareCard(title,body),blob=await new Promise(r=>c.toBlob(r,'image/png')),file=new File([blob],'dre-after-dark-life-os.png',{type:'image/png'});try{if(navigator.canShare?.({files:[file]})){await navigator.share({title:`Dre After Dark · ${title}`,text:body,files:[file]})}else{let a=document.createElement('a');a.download='dre-after-dark-life-os.png';a.href=c.toDataURL('image/png');a.click()}window.DiscoveryEngine.markShared()}catch(e){if(e.name!=='AbortError')console.warn(e)}}function surprise(item){if(!item)return;open(`<div style="text-align:center;padding:2rem"><div style="font-size:4rem">${esc(item.icon)}</div><p class="eyebrow">${item.rare?'RARE DISCOVERY':esc(item.type.toUpperCase())}</p><h2>${esc(item.title)}</h2><p>${esc(item.text)}</p><button id="shareDiscovery">Share this artifact</button></div>`);setTimeout(()=>$('#shareDiscovery')?.addEventListener('click',()=>share(item.title,item.text)),0)}function render(root){let d=window.DiscoveryEngine.state(),season=window.DiscoveryEngine.season(),chron=d.chronicle||[],cab=d.cabinet||[],manual=window.OperatingManual?.model?.(),openQ=manual?.sections?.find(x=>x.name==='Active Questions / Unresolved Evidence')?.items||[];root.innerHTML=`<div class="sectionhead"><div><p class="eyebrow">THE INNER ARCHIVE</p><h2>Chronicle & Cabinet</h2><p>Some things are logged. Others are discovered.</p></div></div>${season?`<article class="card accent"><p class="eyebrow">CURRENT SEASONAL STATE</p><h2>${esc(season.icon)} ${esc(season.title)}</h2><p>${esc(season.line)}</p></article>`:''}${openQ.length?`<article class="card"><p class="eyebrow">ACTIVE QUESTIONS</p><h3>${openQ.length} unresolved ${openQ.length===1?'question':'questions'}</h3><p class="quiet">These are still being tested. They are not operating principles yet.</p>${openQ.slice(0,5).map(x=>`<div class="care-note"><b>${esc(x.claim)}</b><p class="quiet">${esc(x.source)} · evidence remains revisable</p></div>`).join('')}</article>`:''}<div class="grid two"><article class="card"><p class="eyebrow">THE CABINET</p><h3>Unlocked artifacts</h3>${cab.length?cab.map(x=>`<div class="care-note"><b>${esc(x.icon)} ${esc(x.title)}</b><p>${esc(x.text)}</p><button data-share-title="${esc(x.title)}" data-share-body="${esc(x.text)}">Share</button></div>`).join(''):'<p class="quiet">The cabinet is empty. For now.</p>'}</article><article class="card"><p class="eyebrow">THE CHRONICLE</p><h3>Things the archive remembers</h3>${chron.slice(0,40).map(x=>`<div class="task"><small>${new Date(x.at).toLocaleDateString()}</small><br><b>${esc(x.title)}</b><br>${esc(x.text)}</div>`).join('')||'<p class="quiet">Nothing has entered the Chronicle yet.</p>'}</article></div>`;root.querySelectorAll('[data-share-title]').forEach(b=>b.onclick=()=>share(b.dataset.shareTitle,b.dataset.shareBody))}function install(){let nav=$('#nav'),settings=$('#settings');if(!nav||!settings||$('#chronicle'))return;let b=document.createElement('button');b.dataset.view='chronicle';b.textContent='Chronicle';nav.insertBefore(b,nav.querySelector('[data-view="settings"]'));let s=document.createElement('section');s.id='chronicle';s.className='view';s.innerHTML='<div id="chronicleRoot"></div>';settings.parentNode.insertBefore(s,settings);b.onclick=()=>{document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));document.querySelectorAll('#nav button').forEach(x=>x.classList.remove('active'));s.classList.add('active');b.classList.add('active');render($('#chronicleRoot'))};let sea=window.DiscoveryEngine?.season?.();if(sea){document.body.dataset.season=sea.id;document.documentElement.style.setProperty('--season-label',`"${sea.title}"`)}window.addEventListener('lifeos:discovery',e=>{surprise(e.detail);if(s.classList.contains('active'))render($('#chronicleRoot'))});setTimeout(()=>{let found=window.DiscoveryEngine?.evaluate?.()||[];if(found.length)surprise(found[0])},2200)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install):install();window.LifeOSShare={card:shareCard,share};})();