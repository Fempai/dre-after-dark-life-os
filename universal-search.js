/* Dre After Dark · Life OS — Universal Search / Retrieval */
(()=>{'use strict';
const OS='dreLifeOS',$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const read=()=>{try{return JSON.parse(localStorage.getItem(OS)||'{}')}catch{return{}}};
function records(){let d=read(),a=[];(d.signals||[]).forEach(x=>a.push({at:x.at,domain:x.domain||'life',type:x.type||'signal',label:x.label||'',source:x.source||'signals',tags:x.tags||[],id:x.id}));(d.plants||[]).forEach(p=>{a.push({at:p.events?.at(-1)?.at||new Date(0).toISOString(),domain:'plant',type:'specimen',label:p.nickname||'Unnamed specimen',source:'conservatory',tags:[p.status||''],id:p.id});(p.notes||[]).forEach(n=>a.push({at:n.at,domain:'plant',type:'specimen-note',label:`${p.nickname}: ${n.text}`,source:'conservatory',tags:[p.nickname,p.status],id:p.id}))});let c=d.commandCenter||{};['chronicle','cabinet','experiments'].forEach(k=>(c[k]||[]).forEach(x=>a.push({at:x.at,domain:k==='chronicle'?'journal':k==='cabinet'?'archive':'experiment',type:k,label:x.text,source:'command-center',tags:[k],id:x.id})));return a}
function search(q='',opts={}){let needle=String(q).trim().toLowerCase(),from=opts.from?new Date(opts.from).getTime():null,to=opts.to?new Date(opts.to).getTime()+86399999:null,domain=opts.domain||'';return records().filter(x=>{let t=new Date(x.at).getTime(),hay=[x.label,x.domain,x.type,x.source,...(x.tags||[])].join(' ').toLowerCase();return(!needle||hay.includes(needle))&&(!domain||x.domain===domain)&&(!from||t>=from)&&(!to||t<=to)}).sort((a,b)=>new Date(b.at)-new Date(a.at))}
function run(){let q=$('#universalSearchQuery')?.value||'',domain=$('#universalSearchDomain')?.value||'',from=$('#universalSearchFrom')?.value||'',to=$('#universalSearchTo')?.value||'',a=search(q,{domain,from,to}).slice(0,100),h=$('#universalSearchResults');if(!h)return;h.innerHTML=a.length?a.map(x=>`<div class="ii-item"><small>${Number.isFinite(new Date(x.at).getTime())&&new Date(x.at).getTime()>0?new Date(x.at).toLocaleString():'Undated'} · <b>${esc(x.domain)}</b> · ${esc(x.type)}</small><div>${esc(x.label)}</div><small>${esc(x.source)}</small></div>`).join(''):'<p class="quiet">Nothing matched that search.</p>'}
function render(){let h=$('#universalSearch');if(!h)return,d=[...new Set(records().map(x=>x.domain).filter(Boolean))].sort();h.innerHTML=`<article class="card accent"><p class="eyebrow">🔎 UNIVERSAL RETRIEVAL</p><h2>Find anything in Life OS</h2><p>Search across unified signals, Conservatory specimens and notes, Chronicle, Cabinet and experiments. Narrow by domain or date when you remember when something happened but not where you filed it.</p><div class="row"><input id="universalSearchQuery" placeholder="Search Life OS…"><select id="universalSearchDomain"><option value="">All domains</option>${d.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('')}</select><button id="universalSearchGo">Search</button></div><div class="row"><label>From <input id="universalSearchFrom" type="date"></label><label>To <input id="universalSearchTo" type="date"></label><button id="universalSearchClear" class="ghost">Clear</button></div><div id="universalSearchResults" class="quiet">${records().length} searchable records indexed locally.</div></article>`;$('#universalSearchGo').onclick=run;$('#universalSearchQuery').onkeydown=e=>{if(e.key==='Enter')run()};$('#universalSearchClear').onclick=()=>{['universalSearchQuery','universalSearchDomain','universalSearchFrom','universalSearchTo'].forEach(id=>{let x=$('#'+id);if(x)x.value=''});render()}}
function install(){let a=$('#archive');if(!a)return false,h=$('#universalSearch');if(!h){h=document.createElement('div');h.id='universalSearch';a.prepend(h)}render();return true}function boot(){let n=0,t=setInterval(()=>{if(install()||++n>30)clearInterval(t)},200)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();window.addEventListener('lifeos:signals',()=>render());window.LifeSearch={search,records};})();